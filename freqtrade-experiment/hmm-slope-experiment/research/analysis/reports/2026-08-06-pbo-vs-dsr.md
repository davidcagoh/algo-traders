# PBO vs DSR head-to-head — HmmSmaSlope family, bull window — 2026-08-06

Companion to `2026-05-10-dsr-analysis.md`. Uses `evaluation.pbo.cscv_pbo` (the deflator the 2026-05-20 carve-out promised to evaluate head-to-head against DSR — see `../wiki/decisions-archive.md`) and `evaluation.dsr.compute_dsr_from_ledger` against the backfilled ledger (`backfill_ledger.py`), which uses the real trial count for this family (N=11, including 1 explicit reconstruction-gap marker — see `analysis/trials.jsonl`), not `len(wallets)`.

## DSR (ledger-backed trial count)

_N_trials=11 (real search size); N_reported=3 (rows shown); SR* (expected max under null) = 2.637_

| Strategy | Sharpe | Skew | Kurt | N | DSR | Verdict |
|---|---:|---:|---:|---:|---:|---|
| HmmSmaSlope | 1.080 | +9.66 | 119.67 | 792 | 0.000 | NOISE |
| HmmSmaSlopeV3 | 0.976 | +9.54 | 123.44 | 792 | 0.000 | NOISE |
| HmmSmaSlopeV2 | 0.881 | +10.09 | 141.63 | 792 | 0.000 | NOISE |

## PBO (CSCV, n_splits=8)

### PBO (CSCV) — HmmSmaSlope family, bull window

| Metric | Value | Reading |
|---|---:|---|
| PBO | 0.171 | fraction of splits where IS-best is below-median OOS |
| N combinations | 70 | C(S, S/2) evaluated |
| Degradation slope | -0.720 | OLS(OOS ~ IS); 1.0 = no overfitting |
| P(loss) | 0.071 | P(OOS metric < 0) |
| Dominance | 0.357 | P(optimized OOS < median non-optimized OOS) |

**Reading:** DSR uses N=11 (real ledger trial count for this family, not `len(wallets)`=3) and `sharpe_var` pulled from the ledger's own recorded Sharpes across the family's 5 strategies/9 completed trials (`compute_dsr_from_ledger`'s default — see `evaluation/dsr.py`), not just the 3 wallets scored here. Both fixes matter: an earlier pass of this script computed `sharpe_var` only from these 3 near-duplicate wallets (Sharpes clustered in [0.88, 1.08]) and got a tiny variance that inflated DSR to a false SIGNAL — the mirror-image of the original under-deflation bug. With both corrected, DSR agrees with `2026-05-10-dsr-analysis.md`'s original near-zero reading: NOISE. PBO independently agrees — PBO=0.171, degradation slope -0.72 (negative: IS-better trials tend to be OOS-worse, a classic overfitting signature), P(loss)=0.071. Net: two deflators that do not share DSR's Cornish-Fisher approximation or PBO's rank-based mechanism converge on the same conclusion for this family — not distinguishable from noise at N=792 daily obs with this much kurtosis (60-340).
